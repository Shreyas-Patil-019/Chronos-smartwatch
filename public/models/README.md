# 3D Smartwatch Model Directory

This directory is designated for 3D assets (GLB / GLTF formats).

In later phases:
Place the final Blender exported smartwatch model at:
`public/models/smartwatch.glb`

Component `src/components/three/WatchModel.jsx` will load this file using `@react-three/drei`'s `useGLTF`.
